apiVersion: v1
kind: Service
metadata:
  name: torinos-api
  namespace: torinos-api
  labels:
    name: torinos-api
    role: web
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8080
  selector:
    name: torinos-api
    role: web
  type: LoadBalancer
